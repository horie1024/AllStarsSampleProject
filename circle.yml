machine:
  java:
    version: openjdk8
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
dependencies:
  override:
    - echo y | android update sdk --no-ui --filter "android-24,build-tools-24.0.0"
deployment:
  master:
    branch: master
    commands:
      - ./gradlew assemble
